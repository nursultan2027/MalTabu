<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proj.changelang">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.FirstSelect1"/>
        <activity android:name=".activities.FirstSelect2"/>
        <activity android:name=".activities.SecondSelect1"/>
        <activity android:name=".activities.SecondSelect2"/>
        <activity android:name=".activities.ShowCategory"/>
        <activity android:name=".activities.ShowCategory2"/>
        <activity android:name=".activities.ShowCategory3"/>
        <activity android:name=".activities.ShowCategory4"/>
        <activity android:name=".activities.ShowCategory5"/>
        <activity android:name=".activities.ShowCategory6"/>
        <activity android:name=".activities.ShowCategory7"/>
        <activity android:name=".activities.ShowDetails"/>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>